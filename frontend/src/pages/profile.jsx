import { useStateContext } from "../context/contextProvider";
import API from "../API";

const Profile = () => {
    const {user} = useStateContext

    return (
        <>

        </>
    );
  };
  
  export default Profile;